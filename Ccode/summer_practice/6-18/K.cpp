#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    int n;
    ll k;
    int kase = 0;
    while(cin >> n >> k){
        int t = k / (n - 1);
        int r = k % (n - 1);
        if(t == 0){
            
        }

    }
    return 0;
}